<div class="container">
  <div class="form-inline">
    <%= form_tag(recommendations_path, :method => 'get', id: 'search-form') do |f| %>
      <div class="form-group">
        <%= text_field_tag :search, params[:search], placeholder: "Search Places", class:"form-control" %>
      </div>
      <%= submit_tag "Search Places", class:"btn-signup" %>
    <% end %>
  </div>
</div>
